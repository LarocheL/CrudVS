@model System.Data.DataTable
@{
    ViewBag.Title = "URSS Manager";
}
<hr /> 
<table id="table" class="table table-bordered table-striped">
    <tr>
        <th>Nom</th>
        <th>Âge</th>
        <th>Ville</th>
        <th></th>
    </tr>
    @for (int i = 0; i < Model.Rows.Count; i++)
    {
        <tr>
            <td>@Model.Rows[i][1]</td>
            <td>@Model.Rows[i][2]</td>
            <td>@Model.Rows[i][3]</td>
            <td>
                <a role="button" class="btn btn-warning" href="@Url.Action("Edit", "URSS", new {@id=@Model.Rows[i][0] })">Modifier</a>
                <a role="button" class="btn btn-danger" href="@Url.Action("Delete", "URSS", new {@id=@Model.Rows[i][0] })">Supprimer</a>
            </td>
        </tr>
    }
</table>

<a role="button" class="btn btn-primary" href="@Url.Action("Create","URSS")">Ajouter</a>